spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/orders_test?sslmode=disable
    driver-class-name: org.postgresql.Driver
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false

app:
  kafka:
    topics:
      orders-created: orders-created-topic
